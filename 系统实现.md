# 系统实现与验收

现场采集值或 CSV 导入后，系统执行数据清洗与边界裁剪，计算融合风险分数，使用 z-score 标记异常传感器，估计 RUL 并给出维护建议；结果写入 `source/database/engine.db`。

验收流程：首屏展示 4 台设备；选择设备查看 24 个采样点趋势；输入六项传感器值运行诊断；CSV 表头使用 `equipment_id,temperature,vibration,pressure,rpm,oil_quality,power`；预测记录可通过 `/api/predictions` 查询。测试位于 `source/tests/`。
