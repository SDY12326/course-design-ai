# 模型训练报告

项目使用 `source/data/preprocess.py` 处理 NASA C-MAPSS FD001，构造 RUL 与故障风险标签。在线服务采用 80 棵树、最大深度 8 的 RandomForestClassifier 输出风险概率，并融合传感器规则与历史窗口 z-score 异常分数。

训练：`python source/model/train.py`；评估：`python source/model/evaluate.py`，结果写入 `logs/eval_result.json`。基于真实 FD001 处理数据的记录指标为 Accuracy=0.9181、Precision=0.6643、Recall=0.9194、F1=0.7713。
