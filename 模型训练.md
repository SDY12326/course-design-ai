# 模型训练报告
## 1 模型任务介绍
本项目针对NASA C‑MAPSS涡轮发动机数据集做故障风险二分类预测。
输入发动机连续50个时间步的传感器时序数据，输出设备存在故障风险的概率，实现提前预警。

## 2 数据集情况
使用经过data/preprocess.py脚本处理后的`processed_industrial_data.csv`。
- 构造RUL剩余使用寿命标签
- RUL≤30标记为故障风险样本，fault_label=1；其余为正常样本0
- 剔除无效传感器字段，传感器特征做标准化归一化
- 按发动机id切分出时序序列样本，序列长度设置50

## 3 网络结构
采用LSTM循环神经网络：
1. LSTM层：输入传感器特征，隐藏维度128，batch_first=True
2. 取时序最后一步隐层输出
3. 全连接线性层输出一维结果
4. Sigmoid激活输出0‑1概率，大于0.5判定为故障样本

## 4 训练参数
- 序列长度seq_len：50
- batch_size：16
- 优化器：Adam，学习率1e‑3
- 损失函数：BCELoss二分类交叉熵
- 训练轮数epoch：6
- 训练集验证集划分比例：8:2

## 5 模型评估指标
使用准确率、精确率、召回率、F1分数、混淆矩阵综合评价模型故障识别效果。
评估脚本`model/evaluate.py`运行后，评估结果输出保存到`logs/eval_result.txt`。

## 6 文件说明
- model/train.py：模型训练主程序，训练完成输出fault_lstm.pt权重文件
- model/evaluate.py：模型评估程序
