# 方案设计

## 业务背景

面向工厂涡轮发动机、泵组和主轴电机的预测性维护，提前识别潜在故障，降低非计划停机损失。

## 技术方案

采用 B/S 架构：HTML/CSS/JavaScript 前端通过 REST API 调用 FastAPI 后端，后端调用时序预处理、RandomForest 风险分类、z-score 异常检测和 RUL 估计模块，SQLite 保存设备、采样与预测记录。

公开数据集使用 NASA C-MAPSS FD001，存放于 `source/data/`。模块源码统一位于 `source/` 目录。
