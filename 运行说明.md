# 运行说明

在项目根目录执行 `.\source\启动系统.ps1`，脚本会安装依赖、预处理 NASA C-MAPSS FD001 数据并启动 FastAPI 服务。浏览器打开 http://127.0.0.1:8000，接口文档在 http://127.0.0.1:8000/docs。

手动运行：`python source/backend/main.py`。数据处理：`python source/data/preprocess.py`，入库：`python source/data/import_to_db.py`。自动化测试：`python -m pytest -q source/tests`。
